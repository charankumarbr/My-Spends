<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/als_complete">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="@color/colorPrimary"
        android:layout_centerHorizontal="true"
        android:textSize="30sp"
        android:layout_marginTop="50dp"/>

    <ProgressBar
        android:id="@+id/asl_pb_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/als_iv_google_logo"
        android:layout_marginBottom="10dp"
        android:text="Sign In to track your spends."
        android:textSize="19sp"
        android:textColor="@color/colorPrimary"/>

    <ImageView
        android:id="@+id/als_iv_google_logo"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/fui_ic_googleg_color_24dp"
        android:layout_above="@+id/als_abtn_login"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"/>

    <android.support.v7.widget.AppCompatButton
        android:id="@+id/als_abtn_login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@android:color/white"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="32dp"
        android:text="Sign in with Google"
        android:textAllCaps="false"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"/>

</RelativeLayout>