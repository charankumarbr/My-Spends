<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <android.support.v7.widget.Toolbar
        android:id="@+id/am_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:minHeight="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:subtitleTextAppearance="@style/ToolbarSubtitleAppearance"/>

    <in.phoenix.myspends.customview.ButteryProgressBar
        android:id="@+id/am_bpb_loading"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@+id/am_toolbar"
        android:visibility="invisible"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/am_bpb_loading">

        <ListView
            android:id="@+id/am_lv_spends"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:dividerHeight="2dp"
            android:divider="@null"
            android:scrollbars="vertical"
            android:cacheColorHint="@android:color/transparent"
            android:smoothScrollbar="true"
            android:listSelector="@android:color/transparent"/>

        <in.phoenix.myspends.customview.CustomTextView
            android:id="@+id/am_ctv_no_spends"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/unable_fetch_spends"
            android:layout_centerInParent="true"
            android:textSize="21sp"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:gravity="center"
            android:textColor="@color/colorPrimary"
            app:fontName="@string/font_noto_sans"
            android:visibility="gone"/>
    </RelativeLayout>

    <ProgressBar
        android:id="@+id/am_pb_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"/>
</RelativeLayout>